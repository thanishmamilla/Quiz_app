// postcss.config.js

module.exports = {
  plugins: [
    // This plugin enables the use of modern CSS features, 
    // including CSS Custom Properties (your :root variables).
    'postcss-preset-env',
  ],
};